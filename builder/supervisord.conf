[supervisord]
nodaemon=true
loglevel=debug

[program:haserver]
redirect_stderr=true
command=/bin/bash -c "/var/haserver/HAServer -bind="${BIND:-:5094}" -config=${CONFIG_PATH:-config/production.gateway.json}"
